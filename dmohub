-- Load UI Library
local Library = loadstring(game:HttpGet("https://raw.githubusercontent.com/x2zu/OPEN-SOURCE-UI-ROBLOX/refs/heads/main/X2ZU%20UI%20ROBLOX%20OPEN%20SOURCE/DummyUi-leak-by-x2zu/fetching-main/Tools/Framework.luau"))()
-- Create Main Window
local Window = Library:Window({
    Title = "dmo hub",
    Desc = "dmo on top",
    Icon = 105059922903197,
    Theme = "Dark",
    Config = {
        Keybind = Enum.KeyCode.LeftControl,
        Size = UDim2.new(0, 500, 0, 400)
    },
    CloseUIButton = {
        Enabled = true,
        Text = "👽"
    }
})

-- Sidebar Vertical Separator
local SidebarLine = Instance.new("Frame")
SidebarLine.Size = UDim2.new(0, 1, 1, 0)
SidebarLine.Position = UDim2.new(0, 140, 0, 0) -- adjust if needed
SidebarLine.BackgroundColor3 = Color3.fromRGB(60, 60, 60)
SidebarLine.BorderSizePixel = 0
SidebarLine.ZIndex = 5
SidebarLine.Name = "SidebarLine"
SidebarLine.Parent = game:GetService("CoreGui") -- Or Window.Gui if accessible


-- Tab
local m = Window:Tab({Title = "หน้าหลัก", Icon = "star"})
local s = Window:Tab({Title = "ตั้งค่า", Icon = "wrench"})
local g = Window:Tab({Title = "สคริปแมพอื่นๆ", Icon = "navigation"})
    -- Button
    m:Button({
        Title = "บิน",
        Desc = "ก็สคริปบินนั่นแหละไอควาย",
        Callback = function()
            loadstring(game:HttpGet("https://rawscripts.net/raw/Universal-Script-fly-v3-44404"))()
            Window:Notify({
                Title = "Button",
                Desc = "Action performed successfully.",
                Time = 3
            })
        end
    })
    -- Button
    m:Button({
        Title = "คำสั่ง",
        Desc = "ก็คำสั่งไงไอควาย",
        Callback = function()
            loadstring(game:HttpGetAsync("https://raw.githubusercontent.com/EdgeIY/infiniteyield/master/source"))();
            Window:Notify({
                Title = "Button",
                Desc = "Action performed successfully.",
                Time = 3
            })
        end
    })
-- =========================
-- ปุ่ม Teleport
-- =========================
local teleportEnabled = false
local floatingTeleportButton

-- ฟังก์ชันเปิด Teleport Mode
local function startTeleport()
    local UIS = game:GetService("UserInputService")
    local player = game.Players.LocalPlayer
    local mouse = player:GetMouse()

    local function onClick()
        if teleportEnabled then
            local targetPos = mouse.Hit.Position + Vector3.new(0, 3, 0) -- ยกขึ้นเล็กน้อย
            local char = player.Character
            if char then
                local hrp = char:FindFirstChild("HumanoidRootPart")
                if hrp then
                    hrp.CFrame = CFrame.new(targetPos)
                end
            end
        end
    end

    -- เชื่อม Event
    local clickConnection = mouse.Button1Down:Connect(onClick)
    
    -- คืนค่าปิด
    return clickConnection
end

local teleportConnection

-- =========================
-- ปุ่มปกติในหมวด Teleport
    m:Toggle({
    Title = "คริป Teleport",
    Desc  = "กดเพื่อเปิด/ปิด Teleport Mode",
    Value = false,
    Callback = function(state)
        teleportEnabled = state
        if state then
            teleportConnection = startTeleport()
        elseif teleportConnection then
            teleportConnection:Disconnect()
            teleportConnection = nil
        end
    end
})

-- =========================
-- ปุ่มลอย
local function createFloatingTeleportButton()
    if floatingTeleportButton then return end

    floatingTeleportButton = Instance.new("TextButton")
    floatingTeleportButton.Size = UDim2.new(0, 140, 0, 50)
    floatingTeleportButton.Position = UDim2.new(0.5, -70, 0.4, 0)
    floatingTeleportButton.AnchorPoint = Vector2.new(0.5, 0)
    floatingTeleportButton.BackgroundColor3 = Color3.fromRGB(0, 170, 255)
    floatingTeleportButton.TextColor3 = Color3.fromRGB(255,255,255)
    floatingTeleportButton.Text = teleportEnabled and "Teleport: ON" or "Teleport: OFF"
    floatingTeleportButton.Parent = FloatingGui
    floatingTeleportButton.Active = true
    floatingTeleportButton.Draggable = true

    floatingTeleportButton.MouseButton1Click:Connect(function()
        teleportEnabled = not teleportEnabled
        floatingTeleportButton.Text = teleportEnabled and "Teleport: ON" or "Teleport: OFF"
        if teleportEnabled then
            teleportConnection = startTeleport()
        elseif teleportConnection then
            teleportConnection:Disconnect()
            teleportConnection = nil
        end
    end)
end


-- =========================
-- ปุ่ม Reduce Graphics V.1
-- =========================
s:Button({
    Title = "ลดกราฟฟิก V.1",
    Desc  = "ทุก Part เรียบเนียน",
    Callback = function()
        for _, obj in ipairs(workspace:GetDescendants()) do
            if obj:IsA("BasePart") then
                obj.Material = Enum.Material.SmoothPlastic
                obj.Reflectance = 0
            elseif obj:IsA("ParticleEmitter") or obj:IsA("Trail") then
                obj.Enabled = false
            end
        end
        print("✅ ลดกราฟฟิก V.1 เรียบร้อย")
    end
})

-- =========================
-- ปุ่ม Reduce Graphics V.2
-- =========================
s:Button({
    Title = "ลดกราฟฟิก V.2",
    Desc  = "เรียบเนียน + ลบหมอกและเอฟเฟกต์",
    Callback = function()
        -- เรียบเนียนเหมือน V.1
        for _, obj in ipairs(workspace:GetDescendants()) do
            if obj:IsA("BasePart") then
                obj.Material = Enum.Material.SmoothPlastic
                obj.Reflectance = 0
            elseif obj:IsA("ParticleEmitter") or obj:IsA("Trail") then
                obj.Enabled = false
            elseif obj:IsA("Decal") or obj:IsA("Texture") then
                obj:Destroy()
            end
        end

        -- ลบหมอก/เอฟเฟกต์จาก Lighting
        local Lighting = game:GetService("Lighting")
        Lighting.FogEnd = 100000
        Lighting.FogStart = 0
        Lighting.GlobalShadows = false
        print("✅ ลดกราฟฟิก V.2 เรียบร้อย")
    end
})

-- =========================
-- ปุ่ม เพิ่มแสงหน้าจอ
-- =========================
s:Button({
    Title = "เพิ่มแสงหน้าจอ",
    Desc  = "หน้าจอสว่างขึ้นเล็กน้อย",
    Callback = function()
        local Lighting = game:GetService("Lighting")
        Lighting.Brightness = (Lighting.Brightness or 2) + 1 -- เพิ่มทีละ 1
        print("✅ เพิ่มแสงเรียบร้อย")
    end
})
local Players = game:GetService("Players")
local RunService = game:GetService("RunService")

local following = nil          -- ผู้เล่นที่กำลังเลือก
local followEnabled = false    -- สถานะเปิด/ปิดบินตาม


-------------------------------
-- ฟังก์ชันดึงรายชื่อผู้เล่น
-------------------------------
local function GetPlayerList()
    local list = {}
    for _, plr in pairs(Players:GetPlayers()) do
        if plr ~= Players.LocalPlayer then
            table.insert(list, plr.Name)
        end
    end
    return list
end


-------------------------------
-- Dropdown เลือกผู้เล่น
-------------------------------
m:Dropdown({
    Title = "เลือกผู้เล่นเพื่อบินตาม",
    List = GetPlayerList(),
    Value = "None",
    Callback = function(choice)
        following = Players:FindFirstChild(choice)
        print("เลือกผู้เล่น:", choice)
    end
})


-------------------------------
-- Toggle เปิด/ปิดการบินตาม
-------------------------------
m:Toggle({
    Title = "บินตามผู้เล่น",
    Desc = "เปิดเพื่อบินตามผู้เล่นที่เลือก",
    Value = false,
    Callback = function(v)
        followEnabled = v
        print("บินตามสถานะ:", v)
    end
})


----------------------------------------
-- อัปเดตรายชื่อผู้เล่นใน Dropdown
----------------------------------------
task.spawn(function()
    while task.wait(3) do
        Tab:Set("เลือกผู้เล่นเพื่อบินตาม", GetPlayerList())
    end
end)


----------------------------------------
-- ระบบบินตามผู้เล่น (Smooth Follow)
----------------------------------------
RunService.RenderStepped:Connect(function()
    if followEnabled and following then
        local targetChar = following.Character
        local myChar = Players.LocalPlayer.Character

        if targetChar and myChar and myChar:FindFirstChild("HumanoidRootPart") then
            local myHRP = myChar.HumanoidRootPart
            local targetHRP = targetChar:FindFirstChild("HumanoidRootPart")

            if targetHRP then
                -- จุดบินตาม: อยู่เหนือหัว + ข้างหลังเล็กน้อย
                local followPos = targetHRP.CFrame * CFrame.new(0, 5, 7)

                -- Smooth Lerp Movement
                myHRP.CFrame = myHRP.CFrame:Lerp(followPos, 0.15)
            end
        end
    end
end)
-- ตัวอย่างปรับตำแหน่งตามโหมด
if followMode == "Default" then
    followPos = targetHRP.CFrame * CFrame.new(0,5,7)
elseif followMode == "OnHead" then
    followPos = targetHRP.CFrame * CFrame.new(0,6,0)
elseif followMode == "Behind" then
    followPos = targetHRP.CFrame * CFrame.new(0,0,4)
elseif followMode == "Front" then
    followPos = targetHRP.CFrame * CFrame.new(0,0,-4)
elseif followMode == "Under" then
    followPos = targetHRP.CFrame * CFrame.new(0,-4,0)
end
-- ตัวแปรเก็บโหมดบินตาม (ต้องประกาศไว้ในสคริปต์บินตามเดิม)
followMode = followMode or "Default"  -- Default เป็นค่าเริ่มต้น

-- Dropdown เลือกโหมดบินตาม
m:Dropdown({
    Title = "เลือกโหมดบินตาม",
    List = {"Default", "OnHead", "Behind", "Front", "Under"},
    Value = "Default",
    Callback = function(mode)
        followMode = mode
        print("โหมดบินตาม:", mode)
    end
})
local Players = game:GetService("Players")
local RunService = game:GetService("RunService")
local LocalPlayer = Players.LocalPlayer

local ESPFolder = Instance.new("Folder", game.CoreGui)
ESPFolder.Name = "PlayerESP"

-------------------------------------
-- ฟังก์ชันสร้าง ESP สำหรับคนๆหนึ่ง
-------------------------------------
local function CreateESP(player)
    if player == LocalPlayer then return end

    local function add()
        if not player.Character then return end
        local char = player.Character
        local hrp = char:FindFirstChild("HumanoidRootPart")
        local hum = char:FindFirstChild("Humanoid")

        if not hrp or not hum then return end

        -- Highlight
        local highlight = Instance.new("Highlight")
        highlight.Name = player.Name .. "_Highlight"
        highlight.Adornee = char
        highlight.FillColor = Color3.fromRGB(255, 50, 50)
        highlight.OutlineColor = Color3.fromRGB(255, 255, 255)
        highlight.Parent = ESPFolder

        -- Billboard GUI
        local Billboard = Instance.new("BillboardGui")
        Billboard.Name = player.Name .. "_ESP"
        Billboard.Adornee = hrp
        Billboard.Size = UDim2.new(0, 200, 0, 50)
        Billboard.AlwaysOnTop = true
        Billboard.Parent = ESPFolder

        -- Name Label
        local nameLabel = Instance.new("TextLabel", Billboard)
        nameLabel.BackgroundTransparency = 1
        nameLabel.Size = UDim2.new(1, 0, 0.5, 0)
        nameLabel.Font = Enum.Font.SourceSansBold
        nameLabel.TextScaled = true
        nameLabel.TextColor3 = Color3.fromRGB(255,255,255)
        nameLabel.TextStrokeTransparency = 0
        nameLabel.Text = player.Name

        -- HP Label
        local hpLabel = Instance.new("TextLabel", Billboard)
        hpLabel.Position = UDim2.new(0, 0, 0.5, 0)
        hpLabel.Size = UDim2.new(1, 0, 0.5, 0)
        hpLabel.BackgroundTransparency = 1
        hpLabel.Font = Enum.Font.SourceSansBold
        hpLabel.TextScaled = true
        hpLabel.TextColor3 = Color3.fromRGB(0,255,0)
        hpLabel.TextStrokeTransparency = 0

        RunService.RenderStepped:Connect(function()
            if hum and hum.Health > 0 then
                hpLabel.Text = math.floor(hum.Health/hum.MaxHealth*100) .. "% HP"
            else
                hpLabel.Text = "Dead"
            end
        end)
    end

    player.CharacterAdded:Connect(function()
        task.wait(1)
        add()
    end)

    if player.Character then
        task.wait(1)
        add()
    end
end


-------------------------------------
-- ฟังก์ชันเปิด ESP
-------------------------------------
function EnableESP()
    for _, obj in pairs(ESPFolder:GetChildren()) do
        obj.Enabled = true
        if obj:IsA("Highlight") then obj.Enabled = true end
    end

    for _, p in pairs(Players:GetPlayers()) do
        CreateESP(p)
    end
end

-------------------------------------
-- ฟังก์ชันปิด ESP
-------------------------------------
function DisableESP()
    for _, obj in pairs(ESPFolder:GetChildren()) do
        obj:Destroy()
    end
end


-------------------------------------
-- ลบ ESP ตอนผู้เล่นออก
-------------------------------------
Players.PlayerRemoving:Connect(function(player)
    local espObj = ESPFolder:FindFirstChild(player.Name .. "_ESP")
    local hiObj = ESPFolder:FindFirstChild(player.Name .. "_Highlight")
    if espObj then espObj:Destroy() end
    if hiObj then hiObj:Destroy() end
end)
local espEnabled = false  -- สถานะเปิด/ปิด ESP

m:Toggle({
    Title = "เปิด/ปิด ESP ผู้เล่น",
    Desc = "ESP ชื่อ + เลือด + Highlight",
    Value = false,
    Callback = function(v)
        espEnabled = v
        print("ESP:", v)
        
        if v then
            EnableESP()
        else
            DisableESP()
        end
    end
})
